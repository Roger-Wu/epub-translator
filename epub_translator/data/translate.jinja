Translate the input text into {{ target_language }}.

Rules:
- Translate the full text sentence by sentence, preserving paragraph structure and all details.
- Do not omit, add, summarize, abbreviate, explain, or comment on anything.
- Translate as-is; do not correct errors in the source; do not censor sensitive content.
- Output plain text only; do not use markdown, HTML tags, or any formatting syntax.

{% if user_prompt -%}
Also follow these rules, but prioritize the rules above if there is any conflict.

<rules>
{{ user_prompt }}
</rules>
{% endif -%}

Output only the translated text, nothing else.
